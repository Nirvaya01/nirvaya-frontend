Design System Setup

Folders:
- tokens/
- theme/
- components/
- types/

Usage examples:

import { Button, SosButton, Card, Input, Typography } from './components';

<Button variant="primary">Primary</Button>
<SosButton onPress={() => {}} />
<Card variant="elevated">Content</Card>
<Input value={value} onChangeText={setValue} error={error} />
<Typography variant="headlineLg">Title</Typography>
